user  nginx;
worker_processes auto;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for" '
                      '"$upstream_response_time"';

    access_log  /var/log/nginx/access.log  main;

    sendfile       off;
    tcp_nopush     on;

    keepalive_timeout  120;

    # server_tokens off;

    gzip on;
    gzip_disable "msie6" "Mozilla/4";
    gzip_min_length 1100;
    gzip_types text/plain text/xml text/css application/x-javascript application/xml application/rss+xml application/ecmascript text/javascript application/javascript application/json;
    gzip_vary on;

    include /etc/nginx/conf.d/*.conf;
}
